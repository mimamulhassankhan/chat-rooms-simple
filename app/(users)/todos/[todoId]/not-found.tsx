import React from 'react';
import './not-found.scss'

const NotFound = () => {
    return (
        <div className='not-found'>
            The todo is not found
        </div>
    );
};

export default NotFound;